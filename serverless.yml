app: jibot
service: jibot

provider:
  name: aws
  runtime: nodejs12.x

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /
          method: post

plugins:
  - serverless-plugin-typescript
